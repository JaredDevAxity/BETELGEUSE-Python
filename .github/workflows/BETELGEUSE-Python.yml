name: Workflow BETELGEUSE

on:
  workflow_call:
    inputs:
      python-version:
        required: true
        type: string

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
